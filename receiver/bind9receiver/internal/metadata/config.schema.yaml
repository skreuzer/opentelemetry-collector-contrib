# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for bind9 metrics.
    type: object
    properties:
      bind9.memory.total:
        description: "Bind9MemoryTotalMetricConfig provides config for the bind9.memory.total metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      bind9.memory.usage:
        description: "Bind9MemoryUsageMetricConfig provides config for the bind9.memory.usage metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      bind9.network.socket.accept:
        description: "Bind9NetworkSocketAcceptMetricConfig provides config for the bind9.network.socket.accept metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      bind9.network.socket.accept.failures:
        description: "Bind9NetworkSocketAcceptFailuresMetricConfig provides config for the bind9.network.socket.accept.failures metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      bind9.network.socket.active:
        description: "Bind9NetworkSocketActiveMetricConfig provides config for the bind9.network.socket.active metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      bind9.network.socket.bind.failures:
        description: "Bind9NetworkSocketBindFailuresMetricConfig provides config for the bind9.network.socket.bind.failures metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      bind9.network.socket.closed:
        description: "Bind9NetworkSocketClosedMetricConfig provides config for the bind9.network.socket.closed metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      bind9.network.socket.connection:
        description: "Bind9NetworkSocketConnectionMetricConfig provides config for the bind9.network.socket.connection metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      bind9.network.socket.connection.failures:
        description: "Bind9NetworkSocketConnectionFailuresMetricConfig provides config for the bind9.network.socket.connection.failures metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      bind9.network.socket.open.failures:
        description: "Bind9NetworkSocketOpenFailuresMetricConfig provides config for the bind9.network.socket.open.failures metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      bind9.network.socket.opened:
        description: "Bind9NetworkSocketOpenedMetricConfig provides config for the bind9.network.socket.opened metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      bind9.network.socket.receive.errors:
        description: "Bind9NetworkSocketReceiveErrorsMetricConfig provides config for the bind9.network.socket.receive.errors metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      bind9.network.socket.send.errors:
        description: "Bind9NetworkSocketSendErrorsMetricConfig provides config for the bind9.network.socket.send.errors metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      bind9.network.traffic:
        description: "Bind9NetworkTrafficMetricConfig provides config for the bind9.network.traffic metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      bind9.resolver.cache.entries:
        description: "Bind9ResolverCacheEntriesMetricConfig provides config for the bind9.resolver.cache.entries metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      bind9.resolver.cache.hits:
        description: "Bind9ResolverCacheHitsMetricConfig provides config for the bind9.resolver.cache.hits metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      bind9.resolver.cache.misses:
        description: "Bind9ResolverCacheMissesMetricConfig provides config for the bind9.resolver.cache.misses metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      bind9.resolver.queries:
        description: "Bind9ResolverQueriesMetricConfig provides config for the bind9.resolver.queries metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      bind9.resolver.responses:
        description: "Bind9ResolverResponsesMetricConfig provides config for the bind9.resolver.responses metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      bind9.resolver.validation.attempt:
        description: "Bind9ResolverValidationAttemptMetricConfig provides config for the bind9.resolver.validation.attempt metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      bind9.resolver.validation.success:
        description: "Bind9ResolverValidationSuccessMetricConfig provides config for the bind9.resolver.validation.success metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      bind9.server.queries:
        description: "Bind9ServerQueriesMetricConfig provides config for the bind9.server.queries metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      bind9.server.responses:
        description: "Bind9ServerResponsesMetricConfig provides config for the bind9.server.responses metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for bind9 metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
